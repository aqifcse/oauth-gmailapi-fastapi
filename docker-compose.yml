version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - CLIENT_ID=<client_id>
      - CLIENT_SECRET=<client_secret>
    volumes:
      - ./app:/app

    network_mode: "host"